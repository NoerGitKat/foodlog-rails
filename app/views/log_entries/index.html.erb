<div class="section">
  <h1 class="title">Log entries &ndash; <%= DateTime.now.to_fs(:short) %></h1>

  <div id="log_entries" class="is-flex">
    <% @log_entries.each do |log_entry| %> <%= render log_entry %> <% end %>
  </div>
</div>

<div class="columns is-centered mt-6">
  <%= link_to "View all entries", archives_index_path, class: "button is-link
  is-outlined is-centered" %>
</div>
