<!DOCTYPE html>
<html>
  <head>
    <title>Foodlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <%= csrf_meta_tags %> <%= csp_meta_tag %>

    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma-rtl.min.css"
    />
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> <%=
    javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
      <%= render "shared/navbar" %> <% if notice.present? %>
      <p class="notification is-link is-light"><%= notice %></p>
      <% end %> <% if alert.present? %>
      <p class="notification is-danger is-light"><%= alert %></p>
      <% end %> <% if content_for(:title) %> <%= yield :title %> <%= yield %> <%
      else %> <%= yield %> <% end %>
    </div>
  </body>
</html>
